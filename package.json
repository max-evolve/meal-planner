{
  "name": "meal-planner",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "copy-images": "mkdir -p dist/images && cp -r public/images/* dist/images/",
    "prebuild": "npm run copy-images"
  },
  "dependencies": {
    "node-fetch": "^3.3.2",
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.6.2",
    "vite": "^4.5.3"
  }
}
